# Template only. Do not put real credentials in this tracked file.
# ASP.NET Core reads these names as environment variables when configured by the host.
ConnectionStrings__DefaultConnection=
SecretToken__Value=
GoogleAI__ApiKey=
